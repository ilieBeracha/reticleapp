/**
 * Training Hooks
 * 
 * NOTE: useTrainingActions has been removed as Training is read-only.
 * Training status changes should happen through admin interfaces, not here.
 */
export { useTrainingDetail } from './useTrainingDetail';
